name: Daily portfolio news

# MANUAL BACKUP ONLY — real scheduler is Vercel native crons (vercel.json).
on:
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    env:
      CRON_SECRET: ${{ secrets.CRON_SECRET }}
    steps:
      - name: Portfolio news daily email
        run: |
          SECRET=$(printf '%s' "$CRON_SECRET" | tr -d '[:space:]')
          code=$(curl -s -o /tmp/out.txt -w "%{http_code}" \
            "https://nexus.evolutionstrategy.com/api/portfolio-news/daily-email?cron_secret=$SECRET")
          echo "portfolio-news/daily-email -> HTTP $code"; cat /tmp/out.txt; echo
          test "$code" -ge 200 -a "$code" -lt 300
